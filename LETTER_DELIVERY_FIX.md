# Letter Delivery Fix

تم إصلاح مشكلة عدم وصول الخطابات إلى حسابات المستأجرين.

## سبب المشكلة
- واجهة الإدارة كانت ترسل `recipientName` فقط في حالة المستأجر المحدد، بدون `recipientId`.
- Endpoint المستأجر كان لا يعتبر `AllTenants` خطاباً مرئياً للمستأجر.
- إنشاء الخطاب لم يكن ينشئ إشعارات مرتبطة بحسابات المستأجرين.

## ما تم إصلاحه
1. السيرفر يحل `recipientName` إلى `tenantId` حقيقي عند إرسال خطاب لمستأجر محدد.
2. `/api/tenant-portal/letters` يعرض:
   - الخطابات الموجهة لكل المستأجرين.
   - الخطابات الموجهة للمستأجر المحدد بواسطة `recipientId` أو `tenantId`.
   - توافق مع أسماء المستأجرين القديمة.
3. عند إرسال خطاب لجميع المستأجرين، يتم إنشاء In-App Notification لكل حساب Tenant فعال.
4. عند إرسال خطاب لمستأجر محدد، يتم إنشاء Notification لحسابه فقط.
5. يتم إرسال FCM Push للحسابات المستهدفة إذا كانت بيانات Firebase مضبوطة.
6. فشل FCM لا يمنع حفظ الخطاب أو ظهوره داخل التطبيق.
7. يتم حفظ الخطابات والإشعارات في قاعدة البيانات بواسطة طبقة persistence الموجودة بالمشروع.

## بعد النشر
- تأكد أن حساب المستأجر مرتبط بـ `app_users.entity_type='tenant'` و `entity_id=<tenant id>`.
- تأكد أن التطبيق يسجل FCM token على `/api/Account/fcm-token`.
- اختبر إرسال خطاب `AllTenants` ثم خطاب `SpecificTenant`.
